<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.account.AccountMapper">

    <select id="findAll" resultType="com.example.demo.account.AccountDto">
        SELECT * FROM account
    </select>

    <select id="findByPage" resultType="com.example.demo.account.AccountDto">
        SELECT * FROM account

        <!-- Add dynamic filters here -->
        <where>
        	1=1
        	<if test="id != null"> AND id = #{id}</if>
<if test="accountNumber != null"> AND accountNumber = #{accountNumber}</if>
<if test="name != null"> AND name = #{name}</if>
<if test="money != null"> AND money = #{money}</if>

        </where>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findById" resultType="com.example.demo.account.AccountDto">
        SELECT * FROM account WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO account (id, accountNumber, name, money)
        VALUES (#{id}, #{accountNumber}, #{name}, #{money})
    </insert>

    <update id="update">
        UPDATE account
        SET id = #{id}, accountNumber = #{accountNumber}, name = #{name}, money = #{money}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM account WHERE id = #{id}
    </delete>

</mapper>
